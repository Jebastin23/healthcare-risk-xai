# Core numerical / config
numpy>=1.24,<2.2
PyYAML>=6.0
pandas>=2.0.0

# Machine learning
scikit-learn>=1.3.0

# Optional: reference SHAP library (the project ships its own from-scratch
# Shapley implementation, so this is not required to run anything).
# shap>=0.44.0

# Visualisation
matplotlib>=3.7.0

# API & web
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
streamlit>=1.33.0

# Testing / quality
pytest>=8.0.0
httpx>=0.27.0
flake8>=7.0.0
black>=24.0.0
mypy>=1.9.0
